# Быстрый старт: Работа над сайтами

*Пошаговая инструкция для нового человека*

---

## Шаг 0: Перед началом работы

### Что нужно получить от клиента

```
□ Бриф (что за компания, цель сайта, аудитория)
□ Тексты для сайта (хотя бы черновые)
□ Логотип (SVG или PNG)
□ Фирменные цвета (или "на ваше усмотрение")
□ 3-5 референсов (сайты которые нравятся)
□ Фото/видео материалы (если есть)
```

⛔ **Нет материалов — не начинаем!**

---

## Шаг 1: Настройка проекта

### 1.1 Создать структуру папок

```bash
mkdir project-name
cd project-name
mkdir -p .brief/references
```

### 1.2 Разложить материалы

```
project-name/
├── .brief/
│   ├── client-brief.md      ← бриф от клиента
│   ├── content.md           ← тексты для сайта
│   └── references/
│       └── screenshot-*.png ← скриншоты референсов
```

### 1.3 Создать CLAUDE.md

```markdown
# Проект: [Название]

## Стек
- Next.js 14 + App Router
- TypeScript
- Tailwind CSS
- Framer Motion

## Цвета
- Основной: [цвет]
- Акцент: [цвет]
- Фон: [цвет]

## Правила
- Mobile-first
- Коммит после каждого компонента
- НЕ переписывать — улучшать существующее
```

---

## Шаг 2: Планирование (Claude Chat)

### Промпт для Claude Chat:

```
Изучи материалы в .brief/
Предложи:
1. Структуру страницы (секции и порядок)
2. Визуальный стиль (на основе референсов)
3. Какие компоненты из библиотеки использовать
Think hard.
```

### Результат:
- Согласованный список секций
- Понимание визуального стиля
- Записать решения в CLAUDE.md

---

## Шаг 3: HTML-мокапы (Claude Chat)

### Для каждой секции:

```
Создай HTML-мокап для секции [название].
Контент возьми из .brief/content.md
Сохрани в .brief/references/[section]-mockup.html
```

### Порядок:
1. hero-mockup.html
2. about-mockup.html
3. services-mockup.html
4. footer-mockup.html

### Проверка:
Открыть каждый мокап в браузере → убедиться что выглядит хорошо.

---

## Шаг 4: Согласование с клиентом

### Показать мокапы:
- Ссылка на файлы
- Скриншоты в мессенджер
- Видео-запись прокрутки

### Получить:
- ✅ Письменное "ок"
- 📝 Список правок (если есть)

⛔ **Нет подтверждения — не переходим к коду!**

---

## Шаг 5: Разработка (Claude Code / Cursor)

### 5.1 Инициализация

```bash
npx create-next-app@latest . --typescript --tailwind --app
npm install framer-motion lucide-react
```

### 5.2 Скопировать компоненты из библиотеки

```
components/ui/
├── AnimatedLines.tsx    ← анимированные линии к карточкам
├── GlassmorphismCard.tsx
└── TechGrid.tsx
```

### 5.3 Разработка секций

**Промпт для каждой секции:**
```
Прочитай CLAUDE.md.
Конвертируй мокап .brief/references/[section]-mockup.html в React.
Используй Tailwind, Framer Motion.
После — коммит.
```

**Порядок:**
1. Header
2. Hero
3. About
4. Services/Products
5. Footer

**После каждой секции:**
- [ ] npm run dev работает
- [ ] Выглядит как мокап
- [ ] Мобильная версия (375px)
- [ ] git commit

---

## Шаг 6: Деплой

### Чеклист:

```
□ Docker build успешен
□ Сайт доступен по URL
□ HTTPS работает
□ Мобильная версия ок
□ Формы работают
□ Meta-теги заполнены
□ Favicon есть
```

---

## ⚠️ Критичные правила

### Правило изменений
```
❌ "Переделай сайт" → непонятно что делать
✅ "Редизайн секции About" → понятно
✅ "Добавь эффект в Hero, не меняя остальное" → понятно
```

**По умолчанию: улучшаем существующее, НЕ переписываем!**

### Escape-контроль
Если Claude пошёл не туда — **Escape сразу**, не ждать.

### Коммиты = чекпоинты
После каждого логического блока — коммит. Сломалось — откат.

---

## Библиотека компонентов

### AnimatedLines — линии к карточкам
**Файл:** `components/ui/AnimatedLines.tsx`

```tsx
<AnimatedLines
  containerRef={containerRef}
  originRef={originRef}
  targetRefs={[ref1, ref2, ref3]}
  colors={['#00ff88', '#3b82f6', '#f59e0b']}
  isInView={isInView}
  pattern="fan"
/>
```

**⚠️ ВАЖНО:** Целевые элементы НЕ должны иметь анимацию x/y — только opacity!

---

## Типичные проблемы и решения

| Проблема | Решение |
|----------|---------|
| Линии не совпадают с карточками | Убрать анимацию x/y у карточек |
| Сервер не отвечает (504) | Перезапустить: `pkill -f next && npm run dev` |
| Стили не применяются | Очистить кэш: `rm -rf .next` |
| Git конфликт | `git stash && git pull && git stash pop` |

---

*Версия 1.0 | Январь 2026*
