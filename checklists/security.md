# Чеклист безопасности сайта с формами заявок

## Критичность
- 🔴 Критично — делать сразу, без этого не запускать
- 🟡 Важно — сделать в первый месяц
- 🟢 Рекомендуется — улучшает защиту

---

## 1. Защита форм от перехвата

### 🔴 CSRF-защита
- [ ] Каждая форма содержит уникальный CSRF-токен
- [ ] Токен генерируется на сервере и проверяется при отправке
- [ ] Токен одноразовый или привязан к сессии
- [ ] Без валидного токена форма не обрабатывается

### 🔴 Валидация на бэкенде
- [ ] Все поля валидируются на сервере (не только на фронте)
- [ ] Email проверяется регулярным выражением
- [ ] Телефон нормализуется и проверяется
- [ ] Текстовые поля очищаются от HTML/JS (санитизация)

### 🟡 Rate Limiting
- [ ] Ограничение заявок с одного IP (например, 5 в минуту)
- [ ] Ограничение заявок на один email (например, 3 в час)
- [ ] Временная блокировка при превышении лимитов

### 🟡 Honeypot-поля
- [ ] Добавлено скрытое поле, невидимое пользователю
- [ ] Если поле заполнено — заявка отклоняется (это бот)

---

## 2. Защита от внедрения скриптов

### 🔴 Content Security Policy (CSP)
- [ ] Настроен заголовок Content-Security-Policy
- [ ] Разрешены только свои скрипты и явно указанные домены
- [ ] Запрещён inline JavaScript (или используется nonce)

### 🔴 X-Content-Type-Options
- [ ] Заголовок `X-Content-Type-Options: nosniff` установлен

### 🔴 X-Frame-Options
- [ ] Заголовок `X-Frame-Options: DENY` или `SAMEORIGIN`

---

## 3. HTTPS и шифрование

### 🔴 SSL/TLS
- [ ] Весь сайт работает только по HTTPS
- [ ] Редирект с HTTP на HTTPS настроен
- [ ] Сертификат валидный (Let's Encrypt)
- [ ] TLS 1.2+ (старые версии отключены)

### 🔴 HSTS
- [ ] Заголовок `Strict-Transport-Security` установлен

---

## 4. Бэкапы

### 🔴 Регулярные бэкапы
- [ ] Ежедневный бэкап базы данных
- [ ] Еженедельный бэкап файлов сайта
- [ ] Бэкапы хранятся отдельно от сервера
- [ ] Проверка восстановления из бэкапа раз в месяц

---

## Быстрый старт: минимум для запуска

1. ✅ HTTPS + HSTS
2. ✅ CSRF-токены на формах
3. ✅ Валидация на бэкенде
4. ✅ CSP-заголовок (хотя бы базовый)
5. ✅ X-Frame-Options + X-Content-Type-Options
6. ✅ Логирование заявок
7. ✅ Бэкапы

---

*Версия 1.0 | Январь 2026*
