# Блок 5: Процесс

*Статус: 🔧 В работе*
*Последнее обновление: 20 февраля 2026*

---

## 5.1 Фазы проекта

Каждый проект проходит четыре фазы. Фазы последовательны, но не водопадные — возврат на предыдущую фазу нормален и ожидаем.

### Фаза 1: Запуск

```
Что происходит: от идеи до первого рабочего блока
Подробно: PROJECT-LAUNCH.md (шаги 0-9)

Результат:
  - GOAL.md утверждён
  - BLOCKS.md с декомпозицией
  - Контракты написаны, стыки проверены
  - Changelog ведётся с первого дня
  - Первый блок реализован

Критерий завершения: первый блок проходит микросимуляции ✅
Типичная длительность: 2-4 часа (Tier 1), 1-2 дня (Tier 2)
```

### Фаза 2: Реализация

```
Что происходит: параллельная реализация блоков

Процесс:
  1. Оркестратор выбирает следующий блок (по слоям снизу вверх)
  2. Генерирует промпт для агента
  3. Агент реализует блок (ПЛАН → код → тесты → ФАКТ)
  4. Ревьюер валидирует (микросимуляции)
  5. Оркестратор проверяет стыки с существующими блоками
  6. Мёрж в main
  7. Повторить для следующего блока

Параллелизм:
  - Блоки в разных слоях → последовательно (зависимости)
  - Блоки в одном слое → параллельно (разные ветки)
  - Максимум параллельных агентов ≈ количество блоков в текущем слое

Критерий завершения: все блоки ✅, все стыки ✅
Типичная длительность: 1-3 дня (Tier 1), 1-4 недели (Tier 2)
```

### Фаза 3: Интеграция

```
Что происходит: сборка системы из готовых блоков

Процесс:
  1. Оркестратор собирает все блоки в систему
  2. Системная валидация (end-to-end сценарии)
  3. Обнаружение эмерджентных проблем
  4. Фиксы: адаптеры, корректировки контрактов, новые блоки
  5. Повторная системная валидация

Типичные проблемы на этой фазе:
  - Каскадная латентность (каждый блок OK, цепочка — медленная)
  - Пропущенный блок (нужен компонент, который не спроектировали)
  - Семантическое несовпадение (формат OK, но данные бессмысленны)

Критерий завершения: GOAL.md — все измеримые критерии выполнены
Типичная длительность: 1 день (Tier 1), 2-5 дней (Tier 2)
```

### Фаза 4: Поддержка

```
Что происходит: проект в продакшене, идут изменения

Процесс:
  - Баг-репорт → оркестратор диагностирует → агент чинит
  - Новая фича → человек решает → декомпозиция → реализация
  - Breaking change → каскадное обновление (02-decomposition, 2.4)

Changelog продолжает вестись. RAG-база растёт.
Новый агент может подключиться к проекту через RAG — 
без чтения всего кода, только контракты + релевантный changelog.

Критерий завершения: проект закрыт или архивирован
```

### Возврат между фазами

```
Фаза 2 → Фаза 1:
  Триггер: декомпозиция оказалась неправильной
  Действие: пересмотреть BLOCKS.md, обновить контракты
  Правило: не бояться вернуться. Лучше пересмотреть архитектуру 
           на 3-м блоке, чем тащить кривую до конца.

Фаза 3 → Фаза 2:
  Триггер: интеграция выявила пропущенный блок
  Действие: добавить блок в BLOCKS.md, реализовать, вернуться к интеграции

Фаза 4 → Фаза 2:
  Триггер: крупная новая фича требует нескольких блоков
  Действие: мини-цикл реализации внутри поддержки
```

---

## 5.2 Повседневный цикл

Что происходит каждую рабочую сессию, независимо от фазы.

### Начало сессии

```
1. Оркестратор читает:
   - GOAL.md (не изменилась ли цель?)
   - TASKS.md (что открыто, что в работе?)
   - Последние записи CHANGELOG (контекст)
   - Git status (есть ли uncommitted?)
   
2. Если uncommitted изменения:
   → Сообщить человеку первым делом
   → Разобраться: доделать и закоммитить или откатить

3. Определить задачу на сессию:
   - Из TASKS.md (приоритет по слоям и зависимостям)
   - Или от человека (если пришёл с конкретной задачей)

4. ПЛАН в changelog:
   - Что будем делать
   - Зачем (какую цель это приближает)
```

### Работа

```
Цикл:
  1. Взять задачу
  2. Реализовать (или обсудить, или исследовать)
  3. Проверить результат
  4. Закоммитить
  5. Записать в changelog
  6. Следующая задача или завершение

Коммиты: после каждого завершённого логического блока работы.
Написал раздел документа — коммит.
Реализовал блок — коммит.
Мелкие правки — войдут в следующий коммит.
```

### Завершение сессии

```
Триггер: человек говорит "фиксируй день" / "закрываемся"
(или время вышло, или задача выполнена)

1. ФАКТ в changelog:
   - Что сделано
   - Что осталось
   - Отклонения от плана (если есть)
   - Решения (если приняты)

2. Обновить TASKS.md:
   - Закрытые → [x]
   - Новые → добавить
   
3. Git: add, commit, push

4. Краткий итог человеку:
   - Что сделано
   - Что осталось
   - Есть ли блокеры
```

---

## 5.3 Git-стратегия

### Ветвление

```
main
  ├── block/auth          ← агент 1 работает над блоком auth
  ├── block/billing       ← агент 2 работает над блоком billing
  └── block/dashboard     ← агент 3 работает над блоком dashboard

Правила:
  - Каждый блок = своя ветка block/[название]
  - Агент работает ТОЛЬКО в своей ветке
  - Мёрж в main только после:
    а) Микросимуляции пройдены ✅
    б) Стыки с существующими блоками проверены ✅
    в) Changelog-запись (ФАКТ) написана
  - Мёрж делает оркестратор или человек, не агент
```

### Порядок мёржа

```
Мёрж по слоям снизу вверх:
  1. Сначала слой 0 (инфраструктура)
  2. Затем слой 1 (ядро)
  3. Затем слой 2 (бизнес-логика)
  4. Последним — слой 3 (интерфейс)

Внутри слоя: порядок не важен (блоки в одном слое независимы).

Почему: блок зависит от нижних слоёв.
Если нижний слой не в main — верхний не сможет пройти 
интеграционную проверку.
```

### Конфликты

```
Блоки в одном слое НЕ трогают файлы друг друга → конфликтов нет.

Исключения (общие файлы):
  - BLOCKS.md — обновляет оркестратор при мёрже (статус блока)
  - CHANGELOG.md — append-only, конфликты минимальны
  - Shared state — обращение через контракт, не через файл

Если конфликт всё-таки возник:
  1. Оркестратор диагностирует причину
  2. Обычно: неправильные границы блоков (блоки перекрываются)
  3. Решение: пересмотреть декомпозицию
```

### Tier 1: упрощённый git

```
Для 3-5 блоков ветки необязательны.
Можно работать в main напрямую — если один агент в единицу времени.
Ветки нужны когда:
  - 2+ агента работают параллельно
  - Нужна возможность отката отдельного блока
```

### Changelog vs git log

```
Git log фиксирует: ЧТО изменилось в файлах (diff)
Changelog фиксирует: ПОЧЕМУ изменилось и КАКОЙ РЕЗУЛЬТАТ

Git log: "fix: auth token expiration" 
Changelog: "Блок auth v1 → v2: изменён срок жизни токена с 1 часа 
на 24 часа. Причина: пользователи жалуются на частый перелогин. 
Решение согласовано с архитектором. Breaking change: поле expires_in 
теперь в секундах (было в минутах)."

Оба нужны. Они не заменяют друг друга.
```

---

## 5.4 Тестирование

### Принцип из Манифеста

> Тестирование — через поведение. Проверяй не код, а результат. Не «правильный ли синтаксис», а «делает ли блок то, что обещает контракт».

### Три слоя тестирования

```
Слой 1: Контрактные тесты (блочная валидация)
  Что: вход X → выход Y по контракту
  Кто: ревьюер-агент
  Когда: после каждой реализации
  Источник: микросимуляции из контракта (04-validation, 4.2)

Слой 2: Стыковые тесты (интеграция пар)
  Что: выход блока A корректно принимается блоком B
  Кто: оркестратор при сборке
  Когда: при мёрже блока, при breaking change
  Источник: контракты пар блоков

Слой 3: End-to-end тесты (системная валидация)
  Что: пользователь делает X → получает Y
  Кто: оркестратор + человек
  Когда: фаза 3 (интеграция), перед деплоем
  Источник: GOAL.md (измеримые критерии)
```

### Что конкретно тестировать

```
ВСЕГДА тестировать:
  ✅ Основной сценарий (happy path)
  ✅ Невалидный вход (ошибки, пустые данные, wrong types)
  ✅ Граничные случаи (пустой список, максимальный размер, unicode)
  ✅ Соответствие формата выхода контракту

ТЕСТИРОВАТЬ ЕСЛИ ЕСТЬ SLA:
  ✅ Время отклика (p95 < X ms)
  ✅ Пропускная способность (N запросов/сек)
  ✅ Потребление ресурсов (RAM < X MB)

НЕ тестировать:
  ❌ Внутреннее устройство блока (какие функции вызваны)
  ❌ Стиль кода
  ❌ Порядок вызовов внутри блока
  ❌ Конкретные библиотеки, использованные внутри
```

### Инструменты по стеку

```
Тестирование runtime-agnostic по принципу, 
но инструменты зависят от стека блока:

Python блоки:
  - pytest + requests (для HTTP-контрактов)
  - pytest-benchmark (для SLA-метрик)
  - Тесты снаружи блока, обращаются через контракт

Go бинарники:
  - go test (если есть исходник)
  - bash + curl (если бинарник — чёрный ящик)
  - Тесты обращаются через stdin/stdout или HTTP

Фронтенд (HTML/JS):
  - Playwright / Puppeteer (e2e через браузер)
  - Lighthouse (метрики производительности)
  - Ручной чеклист (визуал, мобилка)

Любой блок (универсально):
  - bash-скрипт: отправить вход → проверить выход
  - curl для HTTP-блоков
  - echo + pipe для stdin/stdout-блоков
  
Принцип: тесты обращаются к блоку ЧЕРЕЗ ЕГО КОНТРАКТ.
Если контракт — HTTP, тест шлёт HTTP-запрос.
Если контракт — stdin, тест пишет в stdin.
Инструмент подбирается под транспорт, не под язык.
```

### Кто пишет тесты

```
Микросимуляции (данные для тестов):
  → Оркестратор при создании контракта

Исполняемые тесты (код, который прогоняет данные):
  → Кодер-агент как часть реализации блока
  
  ИЛИ
  
  → Генерируются автоматически из контракта (4.6, уровень 2+)

Ревьюер:
  → Прогоняет тесты, НЕ пишет их
  → Но может добавить тест, если нашёл незакрытый случай

Человек:
  → Не пишет тесты
  → Утверждает что тестировать (через контракт и микросимуляции)
```

---

## 5.5 Дизайн до реализации

### Проблема

UI-блоки (фронтенд, дашборды, интерфейсы) нельзя реализовать по текстовому контракту — нужен визуал. Если агент-кодер получает только "сделай дашборд с таблицей заказов", результат непредсказуем.

### Принцип

```
Для UI-блоков: дизайн — часть контракта.
Агент получает не только вход/выход, но и визуальный референс.

Текстовый контракт отвечает: ЧТО блок показывает и КАК себя ведёт.
Дизайн отвечает: КАК блок ВЫГЛЯДИТ.
```

### Спектр дизайна

```
Уровень 0: Текстовое описание
  "Таблица с колонками: дата, заказ, сумма, статус. 
   Сортировка по дате. Пагинация по 20 записей."
  Работает для: внутренних инструментов, прототипов
  Проблема: каждый агент "увидит" это по-разному

Уровень 1: Wireframe (схема)
  Прямоугольники, стрелки, подписи. Без цветов и стилей.
  Инструменты: Excalidraw, Figma (простой), бумага + фото
  Работает для: Tier 1, MVP, инструменты для внутреннего использования
  Что даёт: структура и расположение, но не финальный вид

Уровень 2: Мокап (статичный дизайн)
  Полноценный дизайн: цвета, шрифты, отступы, иконки.
  Инструменты: Figma
  Работает для: клиентские проекты, Tier 2+
  Что даёт: агент получает точный визуал и воспроизводит его

Уровень 3: Дизайн-система
  Набор компонентов: кнопки, инпуты, карточки, типографика.
  Агент собирает UI из готовых компонентов.
  Работает для: Tier 3, продуктовые компании
  Что даёт: консистентность между блоками, скорость
```

### Процесс для UI-блока

```
1. Человек определяет контракт UI-блока:
   - Какие данные отображает (вход)
   - Какие действия пользователя обрабатывает (выход)
   - Какой уровень дизайна нужен

2. Дизайн создаётся ДО реализации:
   - Уровень 0-1: оркестратор или человек
   - Уровень 2-3: дизайнер (человек или AI-инструмент)

3. Дизайн прикладывается к контракту:
   contracts/dashboard.md → ссылка на мокап / wireframe
   ИЛИ
   contracts/dashboard.md + .brief/references/dashboard-mockup.png

4. Агент-кодер получает:
   - CONTRACT.md (данные, поведение)
   - Визуальный референс (как выглядит)
   - Компоненты (если уровень 3)

5. Ревьюер проверяет:
   - Поведение по контракту (как обычно)
   - Визуальное соответствие мокапу (pixel-perfect не требуется, 
     но структура и элементы должны совпадать)
```

### Когда дизайн не нужен

```
- Бэкенд-блоки (нет UI)
- CLI-блоки (текстовый вывод)
- API-блоки (JSON)
- Бинарные блоки без интерфейса

Правило: если у блока нет визуального вывода для человека — 
дизайн не нужен. Контракта достаточно.
```

### Дизайн и Landing Factory

```
Для лендингов (Tier 1):
  - Библиотека скелетов = уровень 2 (готовые мокапы секций)
  - Оператор выбирает скелет, не рисует с нуля
  - Wow-фишки — отдельный слой поверх скелета
  - Агенту не нужен дизайнер — нужен каталог компонентов

Для клиентских проектов (Tier 2+):
  - Figma-мокап от дизайнера ДО разработки
  - Мокап = часть контракта UI-блока
  - Агент получает мокап + контракт → реализует
```

---

## 5.6 Definition of Done

Когда блок/фича/проект считается завершённым.

### Блок — Done

```
  [ ] Реализация завершена
  [ ] Микросимуляции пройдены ✅ (все сценарии из контракта)
  [ ] Стыки с зависимостями проверены ✅
  [ ] Changelog: ФАКТ написан
  [ ] Код в ветке block/[name], готов к мёржу
  [ ] Для UI-блоков: визуальное соответствие мокапу
```

### Фича (набор блоков) — Done

```
  [ ] Все блоки фичи — Done
  [ ] Стыки между блоками фичи проверены ✅
  [ ] End-to-end сценарий фичи работает
  [ ] BLOCKS.md обновлён (статусы)
  [ ] Changelog: сводная запись по фиче
```

### Проект (релиз) — Done

```
  [ ] Все фичи — Done
  [ ] Системная валидация пройдена (GOAL.md критерии ✅)
  [ ] End-to-end сценарии (основные пути пользователя) ✅
  [ ] Деплой выполнен
  [ ] Changelog: запись о релизе
  [ ] Для клиентских проектов: приёмка заказчиком
```

---

## 5.7 Антипаттерны процесса

### ❌ "Сначала код, потом контракты"

```
Проблема: начинаем кодить не определив контракт
Результат: блок делает что-то, но не то, что нужно системе.
           Контракт пишется постфактум, подгоняясь под реализацию.
Правило: контракт ВСЕГДА до кода.
         Даже если кажется что "и так понятно".
```

### ❌ "Один большой мёрж в конце"

```
Проблема: все блоки разрабатываются в ветках, 
          мёржатся в main разом
Результат: конфликты, сломанные стыки, хаос интеграции.
           "Merge hell" — классическая проблема.
Правило: мёрж по слоям снизу вверх, по одному блоку.
         Каждый мёрж — проверка стыков.
```

### ❌ "Агент знает лучше"

```
Проблема: агент-кодер отклоняется от контракта
          ("я нашёл способ лучше")
Результат: блок работает, но не по контракту.
           Стыки ломаются, диагностика сложнее.
Правило: агент реализует КОНТРАКТ. Точка.
         Если контракт плохой — эскалация на оркестратора,
         не самодеятельность.
```

### ❌ "Дизайн потом нарисуем"

```
Проблема: UI-блок реализуется по текстовому описанию
Результат: 3 итерации "не то", "почти", "ну ладно".
           Время потрачено, результат компромиссный.
Правило: для UI-блоков — дизайн ДО реализации.
         Минимум wireframe. Лучше — мокап.
```

### ❌ "Процесс для больших проектов, у нас маленький"

```
Проблема: пропускаем GOAL, контракты, changelog
          для "простого бота из 3 блоков"
Результат: бот вырос до 8 блоков, контекст потерян,
           контракты определены постфактум.
Правило: масштаб процесса адаптируется к тиру.
         Но GOAL + контракты + changelog — ВСЕГДА.
         Для Tier 1 это 30 минут, не 3 дня.
```

---

*Версия 1.0 | 20 февраля 2026*
*Закрывает открытые проблемы из INDEX.md: #5 (дизайн до реализации → 5.5), #6 (конкретика тестирования → 5.4)*
