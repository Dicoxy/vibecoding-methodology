# Дорожная карта запуска проекта

*Пошаговая инструкция: от идеи до первого рабочего блока*
*Версия 1.0 | 19 февраля 2026*

---

## Когда использовать этот документ

Каждый раз, когда начинается новый проект — независимо от размера и типа. Лендинг, бот, платформа, бинарный сервис — процесс запуска одинаковый. Различается глубина: для лендинга хватит 2 часов, для платформы — 1-2 дня.

---

## Обзор шагов

```
Шаг 0: Определить тир .............. 5 минут
Шаг 1: GOAL.md ..................... 15-30 минут
Шаг 2: Материалы и бриф ........... 1-2 часа (или ждём клиента)
Шаг 3: Репозиторий и структура .... 15 минут
Шаг 4: PROJECT INSTRUCTIONS ........ 30-60 минут
Шаг 5: Декомпозиция (BLOCKS.md) ... 30-60 минут
Шаг 6: Контракты ................... 30-90 минут
Шаг 7: Changelog — старт ведения .. 5 минут
Шаг 8: RAG-инфраструктура ......... 15-60 минут (Tier 2+)
Шаг 9: Первый блок в работу ....... →
```

Не все шаги нужны для каждого тира. Таблица ниже.

---

## Шаг 0: Определить тир проекта

Тир определяет глубину подготовки. Ошибка в выборе тира — переусложнение (лечим лендинг как платформу) или недоподготовка (строим платформу без контрактов).

```
Tier 1: 1-5 блоков, один человек
  Примеры: лендинг, бот, одностраничный сервис
  Время жизни: недели — месяцы
  Подготовка: лёгкая (шаги 1-5, 7, 9)

Tier 2: 8-15 блоков, 2-3 человека
  Примеры: CRM, платформа управления, API-сервис
  Время жизни: месяцы — годы
  Подготовка: полная (все шаги)

Tier 3: 40+ блоков, команда
  Примеры: enterprise-система, маркетплейс
  Время жизни: годы
  Подготовка: полная + дополнительная инфраструктура
```

### Таблица обязательности шагов

```
Шаг                          Tier 1    Tier 2    Tier 3
─────────────────────────────────────────────────────────
0. Определить тир             ✅         ✅         ✅
1. GOAL.md                    ✅         ✅         ✅
2. Материалы и бриф           ✅         ✅         ✅
3. Репозиторий и структура    ✅         ✅         ✅
4. PROJECT INSTRUCTIONS       упрощ.    ✅         ✅
5. Декомпозиция (BLOCKS.md)   упрощ.    ✅         ✅
6. Контракты                  минимум   ✅         ✅
7. Changelog — старт          ✅         ✅         ✅
8. RAG-инфраструктура         ─         ✅         ✅
9. Первый блок в работу       ✅         ✅         ✅
```

**Не уверен в тире?** Начни с Tier 1. Если в процессе декомпозиции получается больше 5 блоков — переходи на Tier 2.

---

## Шаг 1: GOAL.md

**Кто:** Человек-архитектор (с помощью оркестратора)
**Время:** 15-30 минут
**Результат:** файл `GOAL.md` в корне проекта

### Что фиксируем

```markdown
# GOAL — [Название проекта]

## Цель
[Одно предложение: что должно работать на выходе]

## Измеримые критерии
1. [Как узнаем что цель достигнута — конкретная метрика]
2. [Вторая метрика]
3. [Третья метрика]

## Не-цели
- [Что мы НЕ делаем — границы проекта]

## Контекст
- Заказчик: [кто]
- Аудитория: [для кого]
- Сроки: [когда]
- Бюджет: [ресурсы]
```

### Принцип

GOAL.md — якорь проекта. Всё остальное проверяется против него. Если не можешь сформулировать цель в одном предложении — проект не готов к запуску.

### Стоп-критерий

⛔ Нет GOAL.md — не переходим к следующим шагам.

---

## Шаг 2: Материалы и бриф

**Кто:** Человек-архитектор
**Время:** зависит от проекта (от 30 минут до ожидания клиента)
**Результат:** каталог `.brief/` с материалами

### Что собираем

```
Для любого проекта:
  [ ] Описание задачи (что строим)
  [ ] Ограничения (технические, бизнесовые)
  [ ] Существующая инфраструктура (серверы, домены, API)

Для клиентского проекта (дополнительно):
  [ ] Бриф клиента (компания, цель, аудитория)
  [ ] Контент (тексты, данные)
  [ ] Визуальные материалы (логотип, фирменный стиль)
  [ ] Референсы (примеры, которые нравятся)

Для внутреннего проекта:
  [ ] Спецификация (SPEC.md)
  [ ] Интеграции (с какими системами стыкуется)
  [ ] Данные (форматы, объёмы, источники)
```

### Структура .brief/

```
.brief/
├── client-brief.md        ← бриф (если клиентский проект)
├── content.md             ← контент / спецификация
├── references/            ← скриншоты, примеры, мокапы
└── assets/                ← логотип, медиа, данные
```

### Стоп-критерий

⛔ Минимальный набор не собран — не начинаем. Что именно минимально — зависит от проекта, но GOAL.md + описание задачи обязательны всегда.

---

## Шаг 3: Репозиторий и структура

**Кто:** Человек-архитектор (или оркестратор через MCP)
**Время:** 15 минут
**Результат:** репозиторий с базовой структурой

### Создание

```bash
# GitHub: создать репозиторий [project-name]
git clone https://github.com/[user]/[project-name]
cd [project-name]
```

### Базовая структура (все тиры)

```
[project-name]/
├── GOAL.md                  ← из шага 1
├── CLAUDE.md                ← PROJECT INSTRUCTIONS (шаг 4)
├── .brief/                  ← из шага 2
│   ├── content.md
│   └── references/
└── changelog/               ← пустой каталог (шаг 7)
    └── .gitkeep
```

### Расширенная структура (Tier 2+)

```
[project-name]/
├── GOAL.md
├── CLAUDE.md
├── BLOCKS.md                ← граф блоков (шаг 5)
├── .brief/
│   ├── content.md
│   └── references/
├── contracts/               ← контракты блоков (шаг 6)
│   ├── [block-a].md
│   └── [block-b].md
├── changelog/               ← записи по блокам (шаг 7)
│   └── .gitkeep
└── src/                     ← код (появится на шаге 9)
```

### Первый коммит

```bash
git add -A
git commit -m "Инициализация проекта: GOAL, структура, бриф"
git push
```

---

## Шаг 4: PROJECT INSTRUCTIONS (CLAUDE.md)

**Кто:** Человек-архитектор + оркестратор
**Время:** 30-60 минут
**Результат:** файл `CLAUDE.md` — контекст для любого AI-агента, работающего с проектом

### Что содержит

```markdown
# Проект: [Название]

## Цель
[Копия из GOAL.md или ссылка]

## Стек
- [Язык / фреймворк]
- [База данных]
- [Инфраструктура]

## Серверы
- [адрес]: [что на нём]

## Структура
[Описание каталогов и ключевых файлов]

## Правила
- [Критичные правила для агентов]
- Коммит после каждого логического блока
- НЕ переписывать — улучшать существующее
- Changelog по формату (см. 03-changelog-rag.md)

## Зависимости
- [Внешние API, сервисы]
```

### Tier 1: упрощённый вариант

Для простых проектов достаточно: цель, стек, правила, контакты. 10-20 строк.

### Tier 2+: полный вариант

Добавляются: серверы с MCP, ссылки на BLOCKS.md и contracts/, правила changelog-а, git-стратегия (ветки block/*), RAG-инструкции.

### Где размещать

```
Для Claude Code / Cline / Cursor:
  CLAUDE.md в корне репозитория — читается автоматически

Для Claude Chat (оркестратор):
  В Project Knowledge на claude.ai
  + MCP-доступ к серверам для чтения файлов
```

---

## Шаг 5: Декомпозиция (BLOCKS.md)

**Кто:** Оркестратор + человек-архитектор
**Время:** 30-60 минут
**Результат:** файл `BLOCKS.md` — граф блоков проекта

### Процесс

```
1. Оркестратор читает GOAL.md + .brief/
2. Предлагает декомпозицию: список блоков, слои, зависимости
3. Человек утверждает / корректирует
4. Фиксируется в BLOCKS.md
```

### Формат

```markdown
# BLOCKS — [Название проекта]

## Блоки

| Блок | Описание | Контракт | Статус |
|------|----------|----------|--------|
| [name] | [что делает] | contracts/[name].md | 🔲 TODO |

## Зависимости

[block-a] → [block-b]
[block-c] → [block-b]

## Слои

Слой 0 (инфраструктура): [блоки]
Слой 1 (ядро): [блоки]
Слой 2 (бизнес-логика): [блоки]
Слой 3 (интерфейс): [блоки]
```

### Tier 1: упрощённый вариант

Для 1-5 блоков BLOCKS.md может быть списком из 5 строк без формальных слоёв. Главное — зафиксировать что есть и как связано.

### Эвристики декомпозиции

Подробно: `02-decomposition.md`, раздел 2.9. Кратко:
- Одна ответственность = один блок
- Если описание требует "и" — разделяй
- Если два блока всегда меняются вместе — объединяй
- Зависимости только сверху вниз, циклы запрещены

### Стоп-критерий

⛔ Человек не утвердил декомпозицию — не переходим к контрактам.

---

## Шаг 6: Контракты

**Кто:** Оркестратор
**Время:** 30-90 минут (зависит от количества блоков)
**Результат:** файлы `contracts/[block-name].md` для каждого блока

### Процесс

```
1. Для каждого блока из BLOCKS.md:
   а. Оркестратор генерирует контракт по шаблону
   б. Проверяет стыковку с зависимостями
2. Валидация стыков (все пары совместимы?)
3. Человек просматривает критичные контракты
```

### Формат контракта

Подробно: `02-decomposition.md`, раздел 2.2. Кратко:

```markdown
# CONTRACT: [имя блока]
Версия: v1

## Цель
[Зачем блок существует — наследуется от GOAL.md]

## Вход
- Формат: [JSON / binary / ...]
- Транспорт: [HTTP / stdin / function call]

## Выход
- Формат: [...]
- Гарантии: [что всегда возвращается]

## Не делает
- [Границы ответственности]
```

### Tier 1: минимальные контракты

Для простого проекта контракт может быть 5-10 строк: вход, выход, что не делает. Без SLA, без формальных метрик. Главное — зафиксировать границы.

### Tier 2+: полные контракты

Все поля: цель, вход, выход, транспорт, ограничения, зависимости, SLA. Валидация стыков обязательна.

---

## Шаг 7: Changelog — старт ведения

**Кто:** Все участники
**Время:** 5 минут на настройку
**Результат:** changelog начинает вестись с первого дня

### Что сделать

```
1. Создать файл changelog/CHANGELOG.md (или каталог с файлами по блокам)
2. Первая запись:

   ## [project-name] | [YYYY-MM-DD] | архитектор | ПЛАН
   
   ### Цель
   Запуск проекта [название]. Цель: [из GOAL.md]
   
   ### Что планируем
   - Инициализация структуры
   - Декомпозиция на [N] блоков
   - Первый блок в работу: [название]

3. Коммит
```

### Правило

С этого момента каждое изменение фиксируется по формату ПЛАН/ФАКТ (подробно: `03-changelog-rag.md`, раздел 3.2). Без исключений. Первый день задаёт культуру.

---

## Шаг 8: RAG-инфраструктура

**Кто:** Оркестратор (через MCP)
**Время:** 15-60 минут
**Применимость:** Tier 2+ (для Tier 1 — пропустить)

### Что настроить

```
1. Индексатор для changelog
   - Парсинг markdown → чанки
   - Генерация эмбеддингов
   - Загрузка в векторную БД

2. MCP tools для поиска
   - search_changelog — поиск по истории изменений
   - search_contracts — поиск по контрактам (если много блоков)

3. Git hook (post-commit)
   - Автоматическое обновление индекса при коммите в changelog/

4. Проверка
   - Сделать тестовый запрос: "что менялось в блоке [X]?"
   - Убедиться что RAG возвращает релевантный результат
```

### Tier 2: минимальный набор

Индексация changelog + search_changelog через MCP. Остальные слои (verified, known issues) — добавляются по мере необходимости.

### Tier 3: полный набор

Все 6 слоёв знаний (подробно: `03-changelog-rag.md`, раздел 3.4). Автоматические RAG-запросы при создании задачи для агента. Агрегация из распределённых changelog-ов.

---

## Шаг 9: Первый блок в работу

**Кто:** Оркестратор → агент-исполнитель
**Результат:** первый блок реализован, протестирован, закоммичен

### Процесс

```
1. Выбрать блок из BLOCKS.md
   Приоритет: слой 0 (инфраструктура) → слой 1 (ядро) → выше
   Или: блок с минимумом зависимостей

2. Оркестратор генерирует промпт для агента:
   - CLAUDE.md (контекст проекта)
   - CONTRACT.md блока
   - CONTRACT.md зависимостей (если есть)
   - Дизайн/мокап (если UI-блок)

3. Агент пишет ПЛАН в changelog

4. Агент реализует блок
   - Код в ветке block/[название]
   - Тесты (поведение, не реализация)
   - Коммит

5. Агент пишет ФАКТ в changelog

6. Ревью: контракт выполняется? Тесты проходят?

7. Мёрж в main
```

### После первого блока

Проект запущен. Дальше — повторять шаг 9 для каждого следующего блока. Оркестратор координирует порядок, человек контролирует результат.

---

## Чеклист запуска (сводный)

```
ЛЮБОЙ ПРОЕКТ:
  [ ] Тир определён
  [ ] GOAL.md написан и осмыслен
  [ ] Материалы собраны (минимум — описание задачи)
  [ ] Репозиторий создан, базовая структура на месте
  [ ] CLAUDE.md написан
  [ ] Декомпозиция выполнена (хотя бы список блоков)
  [ ] Контракты написаны (хотя бы минимальные)
  [ ] Changelog начат (первая запись)
  [ ] Первый блок в работе

TIER 2+ (дополнительно):
  [ ] BLOCKS.md с графом зависимостей и слоями
  [ ] Контракты полные (вход/выход/транспорт/SLA)
  [ ] Валидация стыков выполнена
  [ ] RAG-инфраструктура настроена и проверена
  [ ] Git-стратегия определена (ветки block/*)
  [ ] PROJECT INSTRUCTIONS содержат RAG-инструкции
```

---

## Типичные ошибки при запуске

```
❌ Начать кодить без GOAL.md
   → Через неделю никто не помнит зачем это строили.
   
❌ Пропустить декомпозицию ("проект простой")
   → Проект оказывается непростым на 3-й день, декомпозировать 
     работающий код больнее, чем спроектировать с нуля.

❌ Не вести changelog с первого дня
   → Через 2 недели контекст потерян. Восстанавливать 
     по памяти — ненадёжно и дорого.

❌ Выбрать слишком высокий тир
   → Бот из 3 блоков не нуждается в RAG, 6 слоях знаний 
     и графе зависимостей. GOAL + контракты + changelog.

❌ Не зафиксировать первый коммит до начала работы
   → Нет точки отката. Первый коммит = структура + GOAL + контракты,
     до единой строки кода.
```

---

*Версия 1.0 | 19 февраля 2026*
*Применять при открытии ЛЮБОГО нового проекта*
