# Блок 3: Changelog + RAG

*Статус: 🔧 В разработке*
*Последнее обновление: 19 февраля 2026*

---

## 3.1 Два слоя документирования

В системе с AI-исполнителями существуют два принципиально разных слоя информации:

```
Слой 1: Артефакты исполнения
  Код, бинарники, конфигурации, тесты.
  Отвечают на вопрос: ЧТО сейчас работает?
  Создаются агентами.
  Заменяемы (весь смысл runtime-agnostic).

Слой 2: Артефакты решений
  Changelog, контракты, GOAL, BLOCKS.
  Отвечают на вопрос: ПОЧЕМУ это работает так?
  Создаются всеми участниками.
  Не заменяемы — это память системы.
```

### Почему слой 2 критичен

**AI не помнит между сессиями.** Каждая новая сессия оркестратора или агента начинается с чистого контекста. Без changelog предыдущая сессия как будто не существовала. Решения, принятые вчера, принимаются заново. Ошибки, найденные на прошлой неделе, повторяются.

**Бинарные блоки нечитаемы.** Когда внутри блока скомпилированный бинарник — нет исходного кода, который можно прочитать. Контракт говорит ЧТО блок делает. Changelog говорит ПОЧЕМУ он делает именно так, какие альтернативы рассматривались, какие проблемы были. Без changelog бинарный блок — чёрный ящик без истории.

**Проект растёт — контекст теряется.** На 5-м блоке ещё можно держать всё в голове. На 15-м — уже нет. На 40+ — невозможно. Changelog + RAG масштабируют память системы независимо от размера проекта.

### Принцип Манифеста

> Документирование — до, а не после. CHANGELOG пишется *до* изменения: что меняем, почему, какой ожидаемый результат.

Это не бюрократия. Это создание контекста для исполнителя. Агент, получивший задачу с записью ПЛАН, понимает не только ЧТО делать, но и ЗАЧЕМ. Запись ФАКТ после завершения фиксирует реальность, которую следующая сессия прочитает через RAG.

---

## 3.2 Формат changelog-записи

Changelog-запись создаётся в два этапа: ПЛАН (до работы) и ФАКТ (после).

### ПЛАН (до начала работы)

```markdown
## [block-name] | [YYYY-MM-DD] | [участник] | ПЛАН

### Цель
[Зачем это делается — связь с целью блока и проекта]

### Что планируем
- [Конкретно что будет сделано]

### Контракт
- Вход: contracts/[name].md v[N]
- Выход: [ожидаемое]

### Ожидаемые метрики
- [Что будем проверять: время отклика, корректность, ресурсы]

### Теги
[блок: auth] [тип: реализация] [приоритет: высокий]
```

### ФАКТ (после завершения)

```markdown
## [block-name] | [YYYY-MM-DD] | [участник] | ФАКТ

### Сделано
- [Конкретно что реализовано]

### Контракт
- Вход: соответствует contracts/[name].md v[N] ✅/❌
- Выход: [описание] ✅/❌

### Тесты (поведение)
- [вход X → выход Y]: ✅/❌
- [граничный случай]: ✅/❌
- [метрика скорости]: ✅/❌

### Отклонения от плана
- [Что пошло не так и почему. Если всё по плану — "нет отклонений"]

### Теги
[блок: auth] [тип: реализация] [статус: готов]
```

### Правила записи

```
1. ПЛАН пишется ДО начала работы — фиксирует намерение
2. ФАКТ пишется ПОСЛЕ завершения — фиксирует результат
3. Формат строго по шаблону — RAG индексирует по структуре
4. Тесты описывают ПОВЕДЕНИЕ (вход → выход), не реализацию
5. Отклонения обязательны — это источник обучения системы
6. При повторном изменении блока — новая пара ПЛАН/ФАКТ
7. Теги обязательны — без них RAG не категоризирует запись
```

### Гранулярность

Уровень **решений и поведения**, не редактирования текста.

```
✅ Правильная гранулярность:
  "Блок auth: добавлена валидация JWT с проверкой expiry. 
   Решение: используем RS256, не HS256 — потому что ключи 
   ротируются между сервисами."

❌ Слишком мелко:
  "Поправлена строка 47 в auth.go"

❌ Слишком крупно:
  "Доделан бэкенд"
```

Сигнал правильной гранулярности: запись отвечает на вопрос "почему именно так?" и полезна человеку/AI через 3 месяца.

---

## 3.3 Кто пишет changelog

Каждый участник документирует свой уровень решений.

### Агент-исполнитель

```
Пишет:
  ПЛАН — перед реализацией блока (что собирается делать)
  ФАКТ — после реализации (что получилось, тесты, отклонения)

Не пишет:
  Архитектурные решения (это оркестратор)
  Бизнес-решения (это человек)

Триггер: получил задачу от оркестратора → ПЛАН → работа → ФАКТ → коммит
```

### Оркестратор

```
Пишет:
  Решения по стыкам (почему контракт A v2, а не v1)
  Каскадные обновления (блок X изменился → что обновить)
  Диагностика (блок Y сломался потому что...)
  Решения по декомпозиции (разделили блок Z на два, потому что...)

Не пишет:
  Детали реализации (это агент)
  Бизнес-обоснования (это человек)

Триггер: принял решение → записать сразу, не в конце сессии
```

### Человек-архитектор

```
Пишет:
  Бизнес-решения (почему строим X, а не Y)
  Утверждения (одобрил контракт, принял блок)
  Ретроспективы (что работает, что нет на уровне процесса)
  Стратегические направления (смена приоритетов, новые цели)

Триггер: принял решение → зафиксировать (сам или через оркестратора)
```

### Общее правило

```
Решение принято → запись в changelog СРАЗУ.
Не "запишу потом". Не "в конце сессии". Сразу.

Причина: "потом" теряется контекст решения.
Через час ты помнишь что решил, но не помнишь почему.
Через день не помнишь и что решил.
```

---

## 3.4 RAG-архитектура

### Проблема

Changelog растёт линейно с каждой сессией. Проект на 15 блоков за месяц работы — это сотни записей. Читать целиком при каждом старте сессии — дорого по токенам и времени. Нужен механизм извлечения релевантного контекста.

### Решение: слои знаний + векторный поиск

RAG (Retrieval-Augmented Generation) — механизм, при котором вместо загрузки всего контекста в окно AI, релевантные фрагменты находятся через поиск и подгружаются по запросу.

```
БЕЗ RAG:
  Оркестратор читает весь CHANGELOG (500+ записей, 50k+ токенов)
  → контекстное окно забито историей
  → на задачу остаётся мало места
  → дорого, медленно, неточно

С RAG:
  Оркестратор запрашивает: "что менялось в блоке auth?"
  → RAG возвращает 3-5 релевантных записей (2-3k токенов)
  → контекст точный и компактный
  → дёшево, быстро, релевантно
```

### Шесть слоёв знаний

Проектная документация разбивается на слои. Каждый слой отвечает на свой тип вопросов.

```
┌─────────────────────────────────────────────────────┐
│                   RAG-БАЗА ПРОЕКТА                  │
│                                                     │
│  📋 Слой 1: CHANGELOG                              │
│     Что менялось, когда, почему, кем                │
│     Источник: changelog-записи (ПЛАН/ФАКТ)         │
│                                                     │
│  🧱 Слой 2: Code Blocks (структура блоков)         │
│     Что делает каждый блок, от чего зависит         │
│     Источник: BLOCKS.md + contracts/                │
│                                                     │
│  📐 Слой 3: Workflow (правила работы)               │
│     Как вести changelog, правила коммитов, процесс  │
│     Источник: PROJECT INSTRUCTIONS, WORKFLOW.md     │
│                                                     │
│  📡 Слой 4: API Docs (внешние зависимости)          │
│     Endpoints, параметры, форматы, коды ошибок      │
│     Источник: документация вендоров                 │
│                                                     │
│  🐛 Слой 5: Known Issues (известные проблемы)       │
│     Текущие баги, workaround'ы, ограничения         │
│     Источник: извлекается из changelog              │
│                                                     │
│  ✅ Слой 6: Verified Facts (подтверждённые факты)    │
│     Что работает и НЕ ДОЛЖНО быть сломано           │
│     Источник: ФАКТ-записи с пройденными тестами     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### Метрика

```
Целевая метрика: <10k токенов контекста для любой задачи,
независимо от размера проекта.

Бюджет на задачу:
  Задача (промпт)              ~500 токенов
  Контракт блока               ~500 токенов
  Контракты зависимостей       ~1000 токенов
  RAG: changelog (3-5 записей) ~2000 токенов
  RAG: verified/known issues   ~1000 токенов
  RAG: workflow правила         ~500 токенов
  ─────────────────────────────────────────
  Итого                        ~5500 токенов

  Запас до 10k — на сложные случаи (больше зависимостей,
  длинная история блока).
```

---

## 3.5 Чанкинг и индексация

### Единица индексации — чанк

Чанк — минимальный фрагмент, который RAG возвращает по запросу. Слишком большой чанк — шум в контексте. Слишком маленький — потеря смысла.

### Стратегия чанкинга по слоям

```
Слой 1 (CHANGELOG):
  Чанк = одна пара ПЛАН/ФАКТ (или одна запись, если непарная)
  Метаданные: блок, дата, участник, тип (план/факт), теги
  Почему: пара ПЛАН/ФАКТ — атомарная единица. 
  Разрывать — терять контекст намерение/результат.

Слой 2 (Code Blocks):
  Чанк = описание одного блока из BLOCKS.md + его контракт
  Метаданные: блок, версия контракта, слой, зависимости
  Почему: блок — атомарная единица декомпозиции.

Слой 3 (Workflow):
  Чанк = одно правило или одна секция процесса
  Метаданные: тип (правило/процесс/чеклист)
  Почему: правила нужны точечно, не целым документом.

Слой 4 (API Docs):
  Чанк = один endpoint или одна группа связанных endpoints
  Метаданные: вендор, версия API, тип операции
  Почему: при работе с API нужен конкретный endpoint, не вся документация.

Слой 5 (Known Issues):
  Чанк = одна проблема + workaround
  Метаданные: блок, серьёзность, статус (открыт/решён)
  Почему: проблемы нужны точечно при работе с конкретным блоком.

Слой 6 (Verified Facts):
  Чанк = один факт (блок X работает, тест Y проходит)
  Метаданные: блок, дата верификации, тесты
  Почему: нужен быстрый ответ "можно ли это трогать".
```

### Формат чанка (общий)

```json
{
  "id": "changelog-auth-2026-02-19-fact",
  "layer": "changelog",
  "content": "текст записи",
  "metadata": {
    "block": "auth",
    "date": "2026-02-19",
    "author": "agent-coder",
    "type": "fact",
    "tags": ["auth", "jwt", "реализация"],
    "contract_version": "v2"
  }
}
```

### Обновление индекса

```
Триггеры:
  1. Git post-commit hook — при коммите в changelog/ или docs/
  2. По запросу оркестратора — "переиндексируй блок X"
  3. Периодический (cron) — catch-all для пропущенных изменений

Процесс:
  1. Обнаружено изменение в файле
  2. Парсинг: извлечь новые/изменённые чанки
  3. Генерация эмбеддингов
  4. Обновление векторной базы
  5. Лог обновления (что проиндексировано, когда)

Инкрементальность:
  Не переиндексировать всё при каждом изменении.
  Только новые/изменённые чанки.
```

---

## 3.6 Паттерны запросов к RAG

### Кто и когда запрашивает

```
АГЕНТ перед началом работы:
  1. "changelog блока [X]"           → история изменений
  2. "known issues блока [X]"        → что сломано/хрупко
  3. "verified facts блока [X]"      → что нельзя ломать
  4. "контракты зависимостей [X]"    → что ожидают соседи

ОРКЕСТРАТОР при планировании:
  1. "все изменения за [дату]"       → статус проекта
  2. "решения по [теме]"             → контекст прошлых решений
  3. "статус блока [X]"              → текущее состояние
  4. "кто зависит от [X]"            → граф для каскадного обновления

ЧЕЛОВЕК при контроле:
  1. "что реально работает"          → verified facts всех блоков
  2. "открытые проблемы"             → known issues, незакрытые
  3. "решения по [теме]"             → почему сделали так
```

### Шаблон автозапроса для агента

При получении задачи агент (или оркестратор от его имени) выполняет стандартный набор RAG-запросов:

```
ПЕРЕД НАЧАЛОМ РАБОТЫ НАД БЛОКОМ [X]:

1. search_changelog("блок [X]")
   → Что менялось раньше, какие проблемы были

2. search_verified("блок [X]") 
   → Что работает и не должно сломаться

3. search_known_issues("блок [X]")
   → Известные проблемы, на которые натыкались

4. search_contracts("зависимости [X]")
   → Контракты блоков, от которых зависит [X]

Результат: 4 запроса → ~3-5k токенов контекста → 
агент знает историю, ограничения и зависимости.
```

### Принцип минимального контекста

```
Не загружай всё что может быть полезно.
Загружай только то, что нужно для текущей задачи.

Если задача — "добавить поле в контракт auth":
  ✅ changelog auth, контракт auth, зависимости auth
  ❌ changelog billing, контракт notifications, вся история проекта

Если задача — "почему система медленная":
  ✅ known issues всех блоков, метрики, последние изменения
  ❌ контракты, verified facts, workflow
```

---

## 3.7 Масштабирование по тирам

### Tier 1 (1-5 блоков, один человек)

```
CHANGELOG: один файл, 50-200 записей за проект
RAG: не нужен. Grep + Ctrl+F достаточно.
Файл читается целиком при старте сессии.

Инструменты: CHANGELOG.md в репозитории.
```

### Tier 2 (8-15 блоков, 2-3 человека)

```
CHANGELOG: один файл или каталог changelog/ с файлом на блок.
  Сотни записей за месяц работы.
RAG: обязателен. Чтение целиком дорого и неточно.
Один индекс, все 6 слоёв.

Инструменты: 
  Индексатор (скрипт парсинга → эмбеддинги → векторная БД)
  MCP tools (search_changelog, search_verified, ...)
  Git hooks (автообновление индекса)
```

### Tier 3 (40+ блоков, команда)

```
CHANGELOG: распределённый. Каждый блок ведёт свой changelog.
  Агрегация через RAG — единая точка поиска по всему проекту.
RAG: обязателен и критичен.
  Возможно несколько индексов (по подсистемам).
  Механизм разгрузки: оркестратор загружает только
  актуальные блоки, не весь граф.

Инструменты:
  Всё из Tier 2 +
  Агрегатор changelog-записей из разных источников
  Дашборд состояния (визуализация статусов блоков)
  Автоматические RAG-запросы при создании задачи для агента
```

### Структура файлов по тирам

```
Tier 1:
  CHANGELOG.md              ← один файл

Tier 2:
  changelog/
  ├── CHANGELOG.md          ← общие записи (решения, ретро)
  ├── auth.md               ← записи блока auth
  ├── billing.md            ← записи блока billing
  └── state-service.md      ← записи state service

Tier 3:
  changelog/
  ├── _aggregated/          ← автогенерация из RAG
  │   ├── daily-2026-02-19.md
  │   └── weekly-2026-W08.md
  ├── subsystem-core/
  │   ├── auth.md
  │   └── billing.md
  └── subsystem-ui/
      ├── dashboard.md
      └── api-gateway.md
```

---

## 3.8 Связь changelog → контракты → GOAL

Changelog не существует в вакууме. Он замыкает цикл обратной связи:

```
GOAL.md (куда идём)
  ↓ определяет
BLOCKS.md (что строим)
  ↓ определяет
contracts/ (как стыкуются)
  ↓ направляет
работа агентов
  ↓ фиксируется в
changelog (что реально произошло)
  ↓ проверяется против
GOAL.md (приближаемся ли к цели?)
```

### Changelog как механизм коррекции

```
Ситуация: в changelog блока billing накопилось 5 записей 
с отклонениями — каждый раз реализация не соответствует плану.

Сигнал: проблема не в реализации, а в контракте или декомпозиции.
Действие: оркестратор эскалирует человеку → пересмотр контракта 
или передекомпозиция блока.

Без changelog этот паттерн невидим — каждая сессия начинается 
с чистого листа, повторяя те же ошибки.
```

### Changelog как защита от сползания

```
Ситуация: агент при реализации блока добавляет "полезную" 
функцию, не описанную в контракте.

Проверка: ФАКТ-запись фиксирует расхождение с ПЛАНОМ.
RAG: при следующей работе с блоком оркестратор видит 
"отклонение: добавлена функция X, не в контракте".
Действие: либо обновить контракт (если функция нужна для GOAL),
либо убрать функцию (если feature creep).
```

---

*Версия 1.0 | 19 февраля 2026*
*Создан на основе: RAG_CONTEXT_DECOMPOSITION.md (концепция), 01-architecture.md секция 1.4 (формат), CHANGELOG решения 19.02.2026*
