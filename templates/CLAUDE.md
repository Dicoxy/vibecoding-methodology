# Проект: [НАЗВАНИЕ КОМПАНИИ]

## ⚠️ КРИТИЧЕСКИ ВАЖНО — ПРОЧИТАЙ ПЕРВЫМ

### Правило изменений
**Улучшаем существующее — НЕ переписываем.**

- Делаем ОДНУ задачу за раз
- После каждой задачи — коммит и СТОП
- Ждём подтверждения перед следующей задачей
- Проверяем что остальные секции НЕ сломались

### Редизайн допустим ТОЛЬКО при:
1. Явном указании — "сделай редизайн [секции]"
2. Поблочном выполнении — одна секция → проверка → следующая
3. Сохранении работающего — если что-то работало, оно продолжает работать

---

## О проекте
[Краткое описание: что за компания, какой стиль сайта]

## Стек
- Next.js 14 (App Router)
- TypeScript  
- Tailwind CSS
- Framer Motion (анимации)
- Docker (деплой)

## Структура секций
```
1. Header — навигация
2. Hero — главный экран
3. About — о компании
4. Products/Services — продукты или услуги
5. [Дополнительные секции]
6. Footer — подвал
```

## Цветовая схема
- Фон: [основной], [вторичный]
- Акцент: [цвет]
- Текст: [основной], [вторичный], [приглушённый]

## Шрифты
- Заголовки: [шрифт]
- Текст: [шрифт]

---

## Команды
```bash
npm run dev      # Локальная разработка
npm run build    # Сборка
npm run start    # Запуск production

# Docker
docker compose build
docker compose up -d
```

## Серверы
- **Dev:** [IP] — порт [порт]
- **Prod:** [IP] — [домен]

## Контакты компании
- Название: [юрлицо]
- ИНН: [инн]
- Email: [email]
- Локация: [город]

---

## Текущая задача
> Получи задачу от человека. Делай ТОЛЬКО её. После — коммит и СТОП.

---

## Библиотека компонентов

Готовые компоненты для копирования:
https://github.com/Dicoxy/vibecoding-methodology/tree/main/components

### AnimatedLines — линии к карточкам
```tsx
<AnimatedLines
  containerRef={containerRef}
  originRef={originRef}
  targetRefs={[ref1, ref2, ref3]}
  colors={['#00ff88', '#3b82f6', '#f59e0b']}
  isInView={isInView}
  pattern="fan"
/>
```

⚠️ **НЕ анимировать x/y целевых элементов** — только `opacity`

---

## ⚠️ Перезапуск после изменений

После любых изменений:
```bash
rm -rf .next
npm run build
pkill -f "next" || true
PORT=[порт] nohup npm run start > /tmp/next.log 2>&1 &
```
